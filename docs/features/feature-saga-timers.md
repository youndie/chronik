---
id: feature-saga-timers
title: "Таймеры саги: ожидание до срока, истечение шага, отмена как компенсация"
type: feature
status: draft
owner: unassigned
involved_services:
  - petich-chronik
  - chronik-core
client_entries: []
api: []
tags: [integration]
---

# Таймеры саги

## 1. Обзор

Отдельный модуль в репозитории саги, чтобы движок без него не тяжелел. Три места: шаг
`awaitUntil(at)`, срок на шаге, ждущем человека, и отмена таймера как обычная компенсация,
встающая в обратный порядок.

## 2. Что здесь ново, а что уже есть

Проверено до написания, потому что первая формулировка задачи здесь ошибалась.

**Уже есть:** активное истечение саг. `SuspendedPetichSweeper` — фоновый воркер, опрашивает
`findExpired` раз в 30 секунд по умолчанию и вызывает компенсацию сам, никого не дожидаясь. Шаг,
вернувший `Suspend(requiredAction, ttl)`, откатывается по истечении `ttl` независимо от того,
пришёл ли кто-нибудь. Утверждение «сейчас истечение проверяется, когда кто-то пришёл» — неверно.

**Ново — три вещи поменьше, и каждая стоит своей цены:**

| Что | Почему это не покрыто существующим |
|---|---|
| Срок на **шаге**, а не на саге целиком | `suspendedUntilEpochMs` — одно поле саги; два шага с разными сроками в него не помещаются |
| **Произвольное событие** в момент срока | подметальщик умеет ровно одно — компенсировать; «напомнить и продолжить ждать» им не выражается |
| Точность, не привязанная к периоду опроса | период опроса подметальщика — 30 секунд по умолчанию, и он общий на все саги |

«Активность» в этот список не входит: она уже оплачена.

## 3. Поток шага `awaitUntil`

1. Интерцептор возвращает результат «ждать до `at`».
2. Мост вызывает `schedule` **в той же транзакции**, в которой пишется состояние саги. Механизм
   тот же, что у outbox-событий движка.
3. Сага паузится, не держа ни потока, ни соединения.
4. В момент `at` событие приходит как обычный «later request», который будит сагу. Механизм
   возобновления не меняется.
5. Откат саги вызывает `cancel` в обратном порядке, как у любого другого шага.

## 4. Якоря

| Модуль | Код |
|---|---|
| petich-chronik | `petich/petich-chronik/src/commonMain/kotlin/` — шаг и компенсация |
| chronik-core | `chronik-core/src/commonMain/kotlin/` — сам таймер |

Что читать прежде, чем писать:

| Репозиторий | Код | Зачем |
|---|---|---|
| petich | `petich/petich-core/src/commonMain/kotlin/SuspendedPetichSweeper.kt` | существующее активное истечение, с которым мост не должен конфликтовать |
| petich | `petich/petich-core/src/commonMain/kotlin/Petich.kt` | `InterceptorResult.Suspend`, `ttl`, возобновление |
| petich | `petich/petich-scheduler/src/commonMain/kotlin/SchedulerWorker.kt` | модуль с перекрывающейся ролью ([B-11](../backlog/B-11-scheduler-boundary.md)) |

## 5. Сценарии

Целевые.

### Scenario: сага просыпается по сроку

* **Given:** сага на шаге `awaitUntil(at)`
* **When:** часы дошли до `at`
* **Then:** сага возобновляется и идёт дальше — **со следующего шага**, потому что `Suspend`
  записывает `index + 1`, и уснувший шаг пробуждения не видит
* **And:** механизм возобновления тот же, что у обычного «later request»
* **And:** возобновление несёт `TimerFired`, иначе движок не отличит его от первого входа
* **Automated:** `SagaTimerSinkTest` (в репозитории движка, ветка `feat/chronik-bridge`)

### Scenario: откат отменяет таймеры саги

* **Given:** сага поставила таймер на шаге N и упала на шаге N+1
* **When:** отработала компенсация
* **Then:** таймер отменён
* **And:** отмена выполнена в обратном порядке наравне с остальными шагами

### Scenario: два шага с разными сроками

* **Given:** сага, у которой шаг A ждёт до `at1`, а шаг B — до `at2`, `at1 < at2`
* **When:** наступил `at1`
* **Then:** сработал только таймер шага A
* **And:** таймер шага B цел и срабатывает в `at2`

Этот сценарий и есть то, чего существующий механизм не выражал: срок жил одним полем саги.

### Scenario: таймер и состояние саги записаны вместе

* **Given:** шаг `awaitUntil`, чья запись состояния провалилась на конфликте версий
* **When:** транзакция откатилась
* **Then:** таймера нет — как нет и outbox-событий этого прохода
* **Automated:** `OneTransactionTest` — против настоящего Postgres, со снятой проверкой на то, что
  обе записи внутри транзакции действительно были

## 5a. Планирование: как таймер попадает в ту же транзакцию

Шаг возвращает `ScheduleTimer` в `sideEffects` своего результата; движок несёт их непрозрачно, а
пишет `ChronikPetichRepository` — в транзакции, которой пишется строка саги. Сам шаг chronik не
зовёт: позвал бы — открыл бы вторую транзакцию.

Точка расширения этого не несла, и её пришлось расширить: до правки `Suspend` не мог передать наружу
ничего, даже outbox-события. Правка сделана в движке (`InterceptorResult.sideEffects`,
`SideEffectAwarePetichRepository`, счётчик потерь и отказ на сборке) — то есть чужой контракт
изменён по решению владельца, а не обойдён.

**Ловушка подключения, найденная прогоном:** блокирующий `transaction {}` и `suspendTransaction {}`
у Exposed друг друга не видят, поэтому вложенный открывает свою транзакцию и коммитит её — таймер
переживает откат состояния. Подключать `suspendTransaction` с обеих сторон.

## 6. Вне охвата

* Подключение у конкретного потребителя — работа в его репозитории.
* Что-либо в модуле расписания: его роль другая и границей D1 отделена.

## 7. Грабли

**Модуль называется `petich-chronik`, а не `petich-timer`.** Имя говорит, к чему он мост, и это
единственная причина: опасение спутать его с существующим модулем расписания снято проверкой на
вехе M3 — роли не пересекаются, граница выписана в D1.

**Соседний модуль расписания решает другую задачу, и путать их дорого.** Он исполняет
повторяющуюся задачу по календарю; chronik сообщает, что пора, и чужого кода не вызывает. Реальный
потребитель уже споткнулся об это в обратную сторону: ему был нужен одноразовый долговечный таймер,
он посмотрел на модуль расписания и написал таймеры в памяти процесса. Таблица границы — в D1.
