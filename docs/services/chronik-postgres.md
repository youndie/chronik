---
id: chronik-postgres
title: chronik-postgres — хранилище на Postgres
type: service
module: chronik-postgres
tech_stack: [Kotlin/JVM, Exposed, PostgreSQL]
owner: unassigned
depends_on:
  - chronik-core
  - PostgreSQL
publishes:
  - "io.github.youndie:chronik-postgres"
---

# chronik-postgres

> **Модуль существует.** Таблица, выборка с арендой и транзакционная запись готовы
> ([B-05](../backlog/B-05-postgres-backend.md), закрыт); доставка с backoff и dead letter — ещё нет
> ([B-06](../backlog/B-06-delivery-sinks.md)).

## 1. Ответственность

Единственный модуль, знающий SQL. Владеет таблицей таймеров: срок, состояние, аренда, счётчик
попыток доставки. Выборка «что пора» и удержание строки за инстансом — здесь.

Чего **не** делает:

- **не создаёт таблиц и не несёт DDL.** Таблицы описывают себя сами, индексы включительно, чтобы
  генератор миграций видел ровно то, по чему фильтруют запросы. Ровно так поступает
  `petich-postgres`, и ровно поэтому потребителю удаётся держать схему в согласии с библиотекой
  (`konekt/server/src/test/kotlin/io/konekt/db/KonektSchemaTest.kt`);
- **не выбирает драйвер и не заводит пул** — принимает готовый `Database`;
- **не растягивает транзакцию на доставку** (D3).

## 2. Контракты

* **Таблица:** `chronik-postgres/src/main/kotlin/TimersTable.kt` — колонки, индексы, имена
* **Реализация хранилища:** `chronik-postgres/src/main/kotlin/ExposedTimerStore.kt`

## 2a. Якоря

| Файл | Что там |
|---|---|
| `chronik-postgres/src/main/kotlin/TimersTable.kt` | таблица с индексом `(state, due_at)`; имена ключа и индекса выводятся из имени таблицы |
| `chronik-postgres/src/main/kotlin/ExposedTimerStore.kt` | запись в чужую транзакцию, выборка `SKIP LOCKED`, аренда |
| `chronik-postgres/src/test/kotlin/SkipLockedTest.kt` | единственное место, где `SKIP LOCKED` действительно проверяется |
| `chronik-postgres/src/test/kotlin/PostgresHarness.kt` | Postgres 18 в контейнере, один на тестовую JVM |

Образцы и проверенные факты:

| Репозиторий | Код | Что оттуда |
|---|---|---|
| petich | `petich/petich-postgres/src/main/kotlin/ExposedPetichRepository.kt` | одна транзакция на бизнес-запись и события; `Dispatchers.IO` вокруг JDBC |
| konekt | `konekt/server/src/main/kotlin/io/konekt/petich/ClaimedSweep.kt` | аренда, написанная потребителем руками, и требование видеть проигравшего |

## 3. Как это устроено

**Две разные вещи, которые легко считать одной.** `SELECT … FOR UPDATE SKIP LOCKED` разводит
инстансы **в момент выборки**; аренда — это колонки `locked_until` / `locked_by`, которые
выбравший проставляет в той же транзакции. Блокировка живёт до конца транзакции, поэтому убитый
инстанс освобождает строку мгновенно, и без колонки не существует никакого «через N секунд станет
снова доступен» — а вся приёмка [B-08](../backlog/B-08-kill-harness.md) сформулирована именно так.

**Наличие API проверено вскрытием артефакта, а не по памяти:**
`ForUpdateOption.PostgreSQL.MODE.SKIP_LOCKED` есть в `exposed-core` 1.4.0
(`org/jetbrains/exposed/v1/core/vendors/ForUpdateOption$PostgreSQL$MODE.class`). Новой зависимости
под конкуренцию инстансов не нужно.

**`Dispatchers.IO` вокруг транзакции — несущая деталь, а не косметика.** Без него транзакция идёт
на том диспетчере, который её вызвал; для маршрутов это прямо потоки движка, а JDBC блокирующий, и
поток, застрявший в нём, соединений не принимает. У соседа это записано как наблюдавшийся исход:
под нагрузкой получались `ConnectTimeout` у клиентов, а не просто медленные ответы.

**Индекс объявляется в определении таблицы, а не только в комментарии.** Не объявленный индекс
уезжает из представления генератора о схеме, и тот требует его удалить у потребителя. Это уже
случалось у соседа и закрывалось отдельным issue.

## 4. Зависимости

| Вид | Имя | Зачем |
|---|---|---|
| База | PostgreSQL | единственный бэкенд v1 (D2) |
| Библиотека | Exposed (`exposed-core`, `exposed-jdbc`) | SQL и `ForUpdateOption` |
| Модуль | [chronik-core](chronik-core.md) | контракты, которые здесь реализуются |

## 6. Локальный запуск

```bash
./gradlew :chronik-postgres:test
```

Требуется работающий docker: контейнер поднимается сам. Настоящий Postgres, а не подмена, —
`SKIP LOCKED`, поведение блокировки и семантика транзакции есть ровно то, что проверяется, и ровно
то, чего у подмены нет. У H2 в режиме совместимости `SKIP LOCKED` отсутствует вовсе.

## 7. Конфигурация

Никакой. `Database` приходит снаружи, имя таблицы — параметр.

## 8. Грабли

**Имена ограничений в Postgres принадлежат схеме, а не таблице.** Имя таблицы здесь — параметр, и
пока имена первичного ключа и индекса были зашиты, две таблицы таймеров в одной схеме не
создавались: вторая падала на `relation "pk_chronik_timers" already exists`. Оба имени теперь
выводятся из имени таблицы; если будете добавлять ещё ограничения — так же, иначе параметр снова
станет обманом.

**Набор тестов может заявлять `SKIP LOCKED` и не проверять его.** Захваты, идущие последовательно,
разводит колонка аренды, а не блокировка строки, поэтому такой набор остаётся зелёным и без
`SKIP LOCKED`. Различие видно только пока чужая транзакция открыта — см. `SkipLockedTest`, и не
удаляйте его как «дублирующий» тест про двух воркеров.
